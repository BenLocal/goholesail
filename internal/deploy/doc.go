// Package deploy holds integration tests that exercise the deployment shell
// scripts (deploy/install.sh and deploy/goholesail-host) from Go, so they run
// under the standard `go test ./...` CI. There is no Go production code here.
package deploy
